package android.app;

import android.content.Context;

/** Compile-time stub; the framework ActivityThread is used at runtime. */
public class ActivityThread {
    public static ActivityThread systemMain() { return null; }
    public Context getSystemContext() { return null; }
}